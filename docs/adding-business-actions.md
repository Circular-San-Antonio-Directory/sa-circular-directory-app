## How to add new Business Actions

| You want to... | What to do |
|---|---|
| Rename an action's display label | Update "Corresponding Action" in Airtable → trigger sync |
| Add a brand new action type | Add to Airtable + add entry to `CSV_ACTION_TO_ACTION_NAME` (maps stable "Action" field → ActionName) |